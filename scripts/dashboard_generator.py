        # Sanitize JSON to prevent XSS in the embedded JS block\n        safe_json = json.dumps(config_data, ensure_ascii=False)\n        \n        # CRITICAL FIX: Escape closing script tags to prevent HTML corruption\n        # If a proxy URL or comment contains </script>, it would break the page.\n        safe_json = safe_json.replace('</script>', '<\\/script>')\n        \n        # Replace placeholder