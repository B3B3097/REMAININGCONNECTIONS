        // Embedded Configuration Data\n        // Escape quotes to prevent XSS and syntax errors\n        const CONFIG_DATA_STR = '{{ CONFIG_JSON | replace(\"'\", \"\\\\'\") }}';\n        let CONFIG_DATA;\n        try {\n            CONFIG_DATA = JSON.parse(CONFIG_DATA_STR);\n        } catch (e) {\n            console.error(\"Failed to parse config data\", e);\n            CONFIG_DATA = {};\n        }\n