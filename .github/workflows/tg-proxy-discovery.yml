*** Begin Patch
*** Update File: .github/workflows/tg-proxy-discovery.yml
@@
-          TG_PROXY_RE = re.compile(r"(?:tg://proxy|https?://t\.me/proxy)\?[^\s\)\]\"'<>]+", re.IGNORECASE)
+          TG_PROXY_RE = re.compile(r"(?:tg://(?:proxy|socks)|https?://t\.me/(?:proxy|socks)|socks5?://|https?://)[^\s\)\]\"'<>]+", re.IGNORECASE)
@@
-          def parse_tg_url(url):
+          def parse_tg_url(url):
               try:
                   parsed = urlparse(url)
                   params = parse_qs(parsed.query)
+                  scheme = parsed.scheme.lower()
+                  kind = (parsed.netloc if scheme == "tg" else parsed.path.strip("/")).lower()
+                  if scheme in ("tg", "http", "https") and kind == "socks":
+                      server = params.get("server", [None])[0]
+                      port = params.get("port", [None])[0]
+                      if not server or not port: return None
+                      port = int(port)
+                      if port <= 0 or port > 65535: return None
+                      if not re.fullmatch(r"[a-zA-Z0-9.-]{3,255}", server): return None
+                      return {"server":server,"port":port,"secret":None,"protocol":"socks5"}
+                  if scheme in ("socks", "socks5"):
+                      if not parsed.hostname or not parsed.port: return None
+                      if parsed.port <= 0 or parsed.port > 65535: return None
+                      return {"server":parsed.hostname,"port":parsed.port,"secret":None,"protocol":"socks5"}
+                  if scheme in ("http", "https") and parsed.hostname and parsed.port and parsed.netloc.lower() != "t.me":
+                      if parsed.port <= 0 or parsed.port > 65535: return None
+                      return {"server":parsed.hostname,"port":parsed.port,"secret":None,"protocol":"http"}
                   server = params.get("server", [None])[0]
                   port = params.get("port", [None])[0]
                   secret = params.get("secret", [None])[0]
@@
-                  return {"server":server,"port":port,"secret":secret}
+                  return {"server":server,"port":port,"secret":secret,"protocol":"mtproto"}
               except Exception:
                   return None
@@
-                  key = f"{parsed['server']}:{parsed['port']}:{parsed['secret']}"
-                  tg_url = f"tg://proxy?server={parsed['server']}&port={parsed['port']}&secret={parsed['secret']}"
-                  tme_url = f"https://t.me/proxy?server={parsed['server']}&port={parsed['port']}&secret={parsed['secret']}"
+                  protocol = parsed["protocol"]
+                  key = f"{protocol}:{parsed['server']}:{parsed['port']}:{parsed['secret'] or ''}"
+                  if protocol == "mtproto":
+                      tg_url = f"tg://proxy?server={parsed['server']}&port={parsed['port']}&secret={parsed['secret']}"
+                      tme_url = f"https://t.me/proxy?server={parsed['server']}&port={parsed['port']}&secret={parsed['secret']}"
+                  elif protocol == "socks5":
+                      tg_url = f"tg://socks?server={parsed['server']}&port={parsed['port']}"
+                      tme_url = f"https://t.me/socks?server={parsed['server']}&port={parsed['port']}"
+                  else:
+                      tg_url = None
+                      tme_url = None
                   sref = {"source":source,"repo":repo,"path":path,"url":curl}
                   if key not in proxies:
-                      proxies[key] = {"server":parsed["server"],"port":parsed["port"],"secret":parsed["secret"],"protocol":"mtproto","tg_url":tg_url,"tme_url":tme_url,"sources":[sref]}
+                      proxies[key] = {"server":parsed["server"],"port":parsed["port"],"secret":parsed["secret"],"protocol":protocol,"tg_url":tg_url,"tme_url":tme_url,"sources":[sref]}
*** End Patch